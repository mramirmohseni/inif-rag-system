[project]
name = "inif-rag-system"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "hazm>=0.12.1",
    "pandas>=3.0.3",
    "psycopg[binary]>=3.3.4",
    "sentence-transformers>=5.6.0",
]
